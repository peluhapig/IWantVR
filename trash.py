  #output = ''
    #whitelist = [
   #     "span"
   # ]

   # for t in text:
 #       if t.parent.name  in whitelist:
 #           output += '{} '.format(t)
 #   print(output)

    #xPath = "/html/body/div[7]/div[1]/div[1]/div[3]/div[1]/div/div/span"
    #text = driver.find_element_by_xpath(xPath).text
    #print(text)

    

#driver.get(url)

#text = driver.find_element_by_xpath(xPath).text
#print(text) 

#if (text != 'Notify When Available'):
    #actual message here but thats a latre problem

    #text = soup.find_all(text=True)
    #span = driver.find_elements_by_tag_name('span')
